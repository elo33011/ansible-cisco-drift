---
# Ansible collections required to run the Cisco drift playbook.
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: cisco.ios
    version: ">=4.6.0"
  - name: ansible.netcommon
    version: ">=5.3.0"
